---
const { label, path, tone = 'cyan' } = Astro.props;
---
<span class:list={['skill-pill', `tone-${tone}`]} title={label}>
  <svg viewBox="0 0 24 24" aria-hidden="true"><path d={path} /></svg>
  <em>{label}</em>
</span>
